import { Box } from "@mui/material";

export default function Filters() {
    return (
        <Box
            sx={{
                border: '1px dashed blue'
            }}
        >
            Filters!
        </Box>
    );
}